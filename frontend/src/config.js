// This file contains the API base URL and endpoints for the application.
export const API_BASE = 'https://ajubuntu:3443';

export const API = {
  RECIPES: `${API_BASE}/recipes`,
  AUTH: `${API_BASE}/auth`,
  CATEGORIES: `${API_BASE}/categories`,
  COMMENTS: `${API_BASE}/comments`,
  FAVORITES: `${API_BASE}/favorites`,
  UPLOADS: `${API_BASE}/uploads`
};
